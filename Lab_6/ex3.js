let zero = 0;
if(zero == []) text='0 is the same as an empty list';
console.log(text);
if(zero == '0') text='0 is the same as the string “0”';
console.log(text);
if([] == zero) text='an empty list is the same as the string “0”';
console.log(text);
